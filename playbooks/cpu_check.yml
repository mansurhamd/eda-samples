---
- name: CPU Warning Alert received
  hosts: localhost
  gather_facts: no
  tasks:
    - name: Show event info
      ansible.builtin.debug:
        msg: "Received CPU_WARN alert from {{ ansible_eda.event.payload.host }} (CPU={{ ansible_eda.event.payload.cpu }}%)"
